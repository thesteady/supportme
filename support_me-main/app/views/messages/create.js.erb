<% publish_to admin_chat_path(@message.chat_id) do %>
  $("#chat<%= @message.chat_id%>").append("<%= escape_javascript render @message %>");
  $("#new_message")[0].reset();
<% end %>


//fix this one too?

<% publish_to admin_chat_path(@chat) do %>
  $("#chat<%= @chat_id%>").append("<%= escape_javascript render partial: 'admin/chats/message', collection: @messages %>");
  $("#new_message")[0].reset();
  $("body").scrollTop( $("body")[0].scrollHeight );
<% end %>
